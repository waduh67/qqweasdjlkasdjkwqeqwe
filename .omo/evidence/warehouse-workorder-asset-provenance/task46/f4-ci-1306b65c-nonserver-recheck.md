Completed independent nonserver artifact verification for CI run36265279847 at `1306b65c34167b2d48f4817ce72990fed3b85aa3`, recorded 2026-09-26T19:32:56.754288+00:00.

Actual reports passed24browser cases across all nine required specs and18spec/project runs, six legacy browser cases,607web assertions in117files,44shared tests and two native compile tasks. All fourteen nonserver jobs succeeded. No failed, skipped, retried or flaky cases were accepted. Fifteen current archives were downloaded/hash-verified, including thirteen authenticated encrypted archives. The complete JSON records each archive identity, report counts and hashes; the earlier incremental-r1 snapshot remains intact.

The V172→178.12 browser upgrade/restart preserves exactly two customers, two ONU identities and all169 historical Flyway checksums. Actual read-only application-role preflight outputs confirm two positive probes and six expected migration/unit/cutover failures. Each upgraded tenant retains customer1/ONU1/movement1/position0 with no verified stock quantity. All367 current migrations and preserved historical source bytes match the tested Git revision.

The actual saved server/web Docker archives match the source revision, tested image/config identities and smoke receipts. Real HTTP, stock reads/writes, original replays, backend/gateway readiness, served web assets and unchanged images after restart are verified. The embedded JAR matches every current browser receipt and the legacy runner, all367 migration bytes and both application configurations. NGINX configuration matches source. The3,693-file Git source manifest exactly matches final-source preparation. No Docker load, image publication or deployment was performed.

This closes current nonserver artifact review only. Current server focused/full/historical evidence and aggregate CI/acceptance remain pending; no final F4 approval is issued. Native compilation does not establish runtime/distribution, and offline GPON evidence does not certify physical devices. Raw XML, authenticated browser traces, fixture data and logs remain private. No local QA or SQL probe was repeated by this reviewer.
