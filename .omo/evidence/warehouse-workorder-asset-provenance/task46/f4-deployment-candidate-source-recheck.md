# Deployment candidate source recheck

The concrete deployment-only candidate closes all four previously reported source findings. No additional high-severity source defect was found in this bounded recheck. Runtime and current complete-CI acceptance remain pending; this is not final F4 approval.

The only differences from the3693-file1306 manifest are five tracked deployment/documentation files and three new shared-proxy files. All3688 other tracked inputs, including application/build/test/workflow sources and all367 migrations, remain byte-identical. `GIT_MASTER=1 git diff --check` passed. The paired JSON records the exact candidate and private verifier/probe hashes; the candidate is uncommitted.

The new init script creates separate migration owner and `warehouse_app` roles using safely quoted psql variables. Runtime is NOSUPERUSER/NOBYPASSRLS/NOCREATEDB/NOCREATEROLE, has no owner membership or schema CREATE, and receives defaults before migration. V178.12 retains its later restrictions; there is no blanket grant after Flyway. Migration-owner BYPASSRLS matches the tested backfill/definer pattern and is not assigned to runtime. Compose now provides distinct Flyway credentials and the canonical demo-password binding while retaining production=true and seed=false. Storage templates and backup-owner documentation agree with these boundaries. Existing installations require an explicitly reviewed ownership migration; this init script intentionally applies only to a new volume.

The shared-proxy overlay uses Compose2.24.4 `!override` to remove public80/443 from the FTTH gateway while retaining the existing optional8880 listener. Only that gateway joins the external edge network under its dedicated alias; server/web/database/storage remain on the FTTH network. The replacement Caddyfile trusts the explicitly configured proxy CIDR and strict forwarded-address parsing. The guide requires an explicit portal URL and both Compose files on every operation, and warns that the existing automatic standalone deployment workflow does not apply this overlay. Actual merged Compose/Caddy validation and HTTPS/existing-site behavior still require root's runtime evidence.

One probe setup defect was identified and corrected during review: `--no-deps` could start the negative server before a new database finished initialization. The current probe explicitly waits with `up -d --wait --wait-timeout180` for postgres, radius-db and MinIO before server startup. It verifies that the negative case differs only by the missing canonical demo credential, requires the actual production-validator error and a failed exit, then starts the positive production configuration without a QA profile. Assertions cover actual runtime role separation, current schema, SELECT-only protected clock tables, real permission denials, platform login, no demo/business fixtures, unchanged-image restart, stable input hashes and owned cleanup. It intentionally does not claim browser journeys, object-storage upload/download or shared-proxy TLS coverage. No probe was executed by this reviewer.

The image importer verification approach is sound for Docker29/containerd: the destination manifest ID may differ from the classic Docker configuration ID. The verifier re-exports the loaded tag, hashes exact configuration bytes and every ordered uncompressed filesystem layer, and compares both archives and observed RootFS layer IDs. A final receipt must preserve1306 source/config IDs, destination IDs, archive hashes and the authenticated transfer-manifest chain. Do not claim the two ID types are numerically equal, rebuild the images, or relabel them as the later configuration revision. This review inspected the verifier source; destination artifacts and its executed receipt are not yet independently authenticated here.

Current unfiltered1306 CI remains untouched. Its actual completed code evidence can be retained for these unchanged application inputs, with a separately versioned/hashes deployment configuration and executed affected runtime checks. A whole-manifest source-equivalence claim would be false after these eight deployment changes. Final review must name both identities and authenticate the remaining server/historical and production-startup evidence.
