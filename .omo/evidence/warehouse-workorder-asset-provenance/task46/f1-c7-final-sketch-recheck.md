# F1 final bounded C7 sketch recheck

No remaining source blocker found in the requested receipt-field patch. This closes the specific source findings from `f1-c7-initial-sql-review.md` and `f1-c7-revised-sql-recheck.md` for the inspected sketch. It is not C7 implementation acceptance, executed migration evidence or final F1 approval.

Private SQL: original-workspace `.omo/runtime/warehouse-draft-expiry.sql`, SHA-256 `add301336feaf41c740f5d6026206500aed07c6bae7c20f168d7aa4ae11adca3`, 542 lines. Recovery copy: validation-worktree `.omo/drafts/warehouse-draft-expiry.sql`, SHA-256 `7ad81ff125e67c415780bd778858676590e6b2cdde3cde07ebfd398c4e59ffea`. The recovery copy has two explicit UNAPPLIED/pending-integration header lines; its remaining bytes exactly match the private sketch. Input hashes and that comparison are recorded in `f1-c7-final-sketch-source.json`.

The source comparison was against validation HEAD `99ad992db31a6922ec1adf1957af81bf747426e6` plus existing C8 changes. No product/test/plan edits, SQL execution, browser activity or other runtime QA were performed by this reviewer.

- The protected activity snapshot now captures the full intake row through a query constrained to the exact tenant/document. The deferred seal compares its snapshot and relational source/inspection location IDs with the original intake identity and canonical request. Those bindings address the remaining relational-intake mismatch.
- Captured lines now bind inspection_required_snapshot, fresh null stock/lot/source identities, zero accepted/rejected/missing quantities and continuous_cut=true. Custody kind, condition and legal owner comparisons are null-safe. These match `WarehouseReceiptPersistence.saveDraft` and the schema defaults.
- Conversion checks correctly expect SQL NULL for SERIAL lines, while other tracking types compare all three numerically parsed conversion columns. This matches the writer's deliberate SERIAL exception. Cost compares numeric totalMinor/basis and exact currency, retaining acceptance of leading-zero input strings without changing the original canonical bytes or hash.
- The earlier fixes remain present: receipt-evidence INSERT liveness; pre-backfill source/operation locks; new movement/effect liveness and deferred idle-source effect exclusion; backfill integrity rejection; stable deadline-based expiry projection; and atomic termination of pending approvals when terminal expiry is persisted. The receipt seal validates the captured save boundary, so a later header change cannot skip it.

Relevant code: `server/src/main/kotlin/com/duluin/ftth/inventory/adapter/outbound/persistence/WarehouseReceiptPersistence.kt:22`; `server/src/main/kotlin/com/duluin/ftth/inventory/application/service/ReceiptDraftValidation.kt:14`; `server/src/main/kotlin/com/duluin/ftth/inventory/domain/model/StockQuantity.kt:65`; `server/src/main/resources/db/migration/V174__warehouse_documents_and_balances.sql:28`. Receipt capture/seal changes are at private sketch lines 242–247 and 325–350; recovery-copy line numbers are two higher.

Required work remains the already planned store.sealDraft integration after original identity persistence, worker/approval termination, read/UI overlays, typed errors, forward-migration/upgrade integration, and executed non-owner raw-write/replay/expiry-race tests. Source review cannot establish those gates. Keep the working SQL outside Flyway until integration is ready; do not mark C7 or final F1 complete from this note.
