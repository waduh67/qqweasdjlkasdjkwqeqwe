# F1 deployment authorization and source addendum at db09c3c2

No additional source blocker was found in the eight deployment/configuration/documentation changes. Final F1, full CI and application activation acceptance remain pending. This addendum preserves the existing 48 task indexes and their historical evidence.

The later user authorization permits actual Azure VPS/domain setup while preserving unrelated Drive/Caddy. Main merge and registry publication remain outside the authorization. Approved credentials stay private; this reviewer did not inspect or record their values and performed no QA, SSH or product edits.

The release now has two identities: application/image source `1306b65c34167b2d48f4817ce72990fed3b85aa3` and deployment configuration `db09c3c2b37ad29b07464b05cd02895265f99a0d`. Exactly five of the 3,693 recorded inputs changed, and three shared-proxy files are new. The other 3,688 inputs and all 367 migrations through 178.12 match. The whole old manifest must not be described as unchanged. Source hashes and actual reference locations are in the companion JSON.

| Plan obligations | Deployment conclusion and evidence boundary |
|---|---|
| C7/C10; tasks 1, 4, 6, 11, 12, 43, 46 | New-volume initialization separates Flyway/database ownership from `warehouse_app`, removes runtime schema CREATE and owner membership, and preserves migration-defined clock restrictions. Production validation remains active. F4 authenticated R2's production-mode bootstrap, missing-secret failure, non-owner denials, all migrations and login/restart. |
| C8/C9/C10; tasks 31, 45–48 | The shared-proxy overlay removes FTTH host 80/443, joins only its gateway to the existing edge network and restricts trusted forwarding to the configured private CIDR. Both Compose files are required; the runbook preserves the existing site and excludes the standalone automatic deploy route on this host. Existing F3 proves application behavior; current host/proxy evidence is a separate boundary. |
| C7/C10; tasks 47–48 | Backup uses the selected PostgreSQL image and migration database owner while preserving archive object ownership/ACLs. Existing installations require separate reviewed ownership migration. Workflow dependencies remain intact, and the old documentation suggesting removal of a required gate is deleted. No executed restore is inferred. |
| C7; tasks 46–48 | Canonical demo-secret binding and stronger storage-key guidance match the existing production validator. The optional mail toggle disables only environment SMTP health. Database health and production validation remain active; health UP does not prove SMTP delivery. |

Bootstrap R1 remains an SMTP-related readiness failure. R2 passed on exact reviewed Compose/init hashes with production=true, no QA profile, no demo/business fixtures, real schema CREATE and policy DELETE denial, platform login and same-image restart. F1 checked the safe receipt/source hash chain and attributes runtime facts to F4's archive/log/source-assertion authentication. Standalone raw HTTP/SQL/inspect responses were not retained, so no independent reparse of those responses is claimed.

The subsequent F4 destination-image proof closes the earlier unavailable-re-export limitation: actual destination configuration bytes and all 19 ordered layers match the authenticated CI images, and OCI manifest identities remain distinct from classic Docker configuration IDs. F1 references that independent artifact review; it did not load images or inspect destination tar payloads. Neither bootstrap nor image identity proves final network operation or production activation.

Root reports eight infrastructure services running, with production backend, web and gateway not started. Network R3 is reported passing while its independent F4 authentication is being completed. Final effective-host evidence must preserve the unrelated Drive site/Caddy, bind all overlays and image identities, verify the intended exposure/TLS behavior and show the reported private non-debug FreeRADIUS command. RADIUS configuration validation does not prove subscriber authentication/accounting.

Remaining required closure is the actual current full server/focused/historical/protocol CI and aggregate result, final independent network and F2/F4 reviews, effective-host preservation/exposure checks, and actual authorized application activation/restart evidence. Infrastructure startup is not application activation. Physical GPON certification remains deferred; native compilation is not a native release; SMTP delivery remains unconfigured. No prior task/index or failure record has been overwritten.
