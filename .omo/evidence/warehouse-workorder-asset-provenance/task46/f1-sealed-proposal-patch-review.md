# Targeted F1 source review: sealed proposal C8 correction

Reviewed validation worktree HEAD `d6baf827ce9bcdae6222acc7c63b8fe64b89fa87` plus the 13 uncommitted files recorded in `f1-sealed-proposal-patch-source.json`. No product/test edits or QA were performed by this reviewer. This is not final F1 approval.

**Result: no blocking defect found in the inspected patch.** The receipt editability and title-correction rework changes implement the previously agreed operational-draft versus sealed-proposal distinction without weakening the immutable source contract. Backend execution is still required; broader asset-exception creation UI, expiry and final validation obligations are not closed by this result.

Paths below are repository-relative; `S` = `server/src/main/kotlin/com/duluin/ftth`, `T` = `server/src/test/kotlin/com/duluin/ftth`.

| Review concern | Source and conclusion |
| --- | --- |
| Authoritative receipt distinction | `S/inventory/application/service/WarehouseReceiptService.kt:114` derives `EDITABLE`, `SEALED_SUPPLIER_REPLACEMENT` or `NOT_DRAFT` from current state and `SupplierReplacementStore.forReceipt`. The latter queries the actual tenant-scoped replacement relationship (`S/inventory/adapter/outbound/persistence/SupplierReplacementStore.kt:33`). No text-prefix, owner, revision-zero or client flag is treated as authority. |
| Current scope and privacy | GET and list retain receipt-view permission and current intake/location authorization before computing the additive flag (`WarehouseReceiptService.kt:78,93`). The new response field reveals no customer/WO/return identifiers, evidence or sealed snapshot. The mutation still locks current authority/topology/resource, checks current old/input locations and actor/key/hash/cutover, and only then handles the new domain restriction (`:36–61`). |
| Fresh replacement PUT | The new seal check runs after expected revision/state checks and before intake preparation or `saveDraft` (`:58–64`). It returns stable `SOURCE_NOT_VERIFIED` with successor-request guidance. No replacement snapshot, identity, line or title edit is introduced. Database immutability remains intact; this adds an application-level rejection instead of depending solely on a deferred constraint failure. |
| Receipt historical replay | Exact command replay still returns `prior.receipt`, after current and original intake scope checks (`:51–56`). The new seal check follows replay, so it cannot reinterpret a previously accepted command as a new edit. Stored command response generation still uses `view`, not `currentView` (`:65`); `ReceiptView.draftEditability` defaults null and has NON_NULL serialization. Thus existing response bodies and source snapshots are not rewritten to carry today's editability. The receipt controller still returns `originalBody` unchanged. |
| New receipt reads and replacement creation | Current GET/list use `currentView`, while command bodies deliberately omit the derived field. This also avoids misclassifying a newly prepared replacement receipt before its relationship is inserted in the same transaction. After commit, current GET derives the sealed state from the persisted relation. No dependency cycle was introduced: the added service dependency is the persistence store, not `SupplierReplacementService`. |
| Web old/missing field behavior | `web/src/api/warehouse/receipts.ts:58` uses nullable enum decoding. `codec.ts:56` maps both absent and null values to null. `WarehouseReceiptsPage.tsx:92` offers edit only for explicit `EDITABLE`, shows reload guidance for null, and removes the irrelevant cost-edit warning for sealed proposals. `WarehouseReceiptEditor.tsx:32` independently rejects non-editable input from a stale/direct caller. Existing create has no receipt prop and remains available. Receive/inspect/putaway controls remain in their existing states. |
| Honest replacement semantics | `WarehouseSupplierReplacement.tsx` changes create/review/success copy from editable draft to recorded proposal, and says physical receipt and applicable approval remain next steps. Receipt detail points operators toward a new request from the original return case. A direct return-case link would improve navigation, but its absence is not a regression or blocker for this targeted removal of a false edit affordance; the existing return workflow already creates successors. |
| Title correction rework | `S/inventory/application/service/WarehouseApprovalQueryService.kt:132` now excludes `TITLE_CORRECTION` from canRework. `DurableApprovalService.kt:182` rejects a fresh direct rework command before changing disposition/revision. `AssetTitleCorrectionOwner` still requires the sealed source at revision zero. New corrected proposals remain the supported path. Existing UI only renders rework when canRework is true and already explains fresh source requests (`WarehouseApprovalsPage.tsx:109–110`). |
| Title historical replay | The existing rework replay remains after current requester/source visibility checks and before the new family rejection (`DurableApprovalService.kt:166–183`). A historical successful rework response may still replay exactly; it does not execute a second header mutation or make that old source newly approvable. The patch makes no migration or rewrite of rejected/history records. |

## Validation evidence and remaining gate

Inspected new tests are meaningful for the change:

- `T/inventory/WarehouseSupplierReplacementGuardsIT.kt:11` checks detail/list editability, direct replacement PUT rejection with unchanged current receipt, continued ordinary draft create/update, omission of the field from stored command bodies, and exact create replay after an edit.
- `T/customer/CustomerAssetOwnershipIT.kt:11` checks rejected title correction canRework=false, direct rework 409 without title mutation, a distinct fresh proposal plus independent final approval, retained old rejected source, and one title-transfer effect.
- `web/src/pages/warehouse/WarehouseReceiptsPage.test.tsx:32` checks both sealed and null/missing-field behavior and direct editor refusal. Replacement UI tests assert the corrected proposal language while keeping the real API command structure.

Parent reported web 14/14 plus typecheck/build PASS in session 5409. This reviewer has not independently inspected those raw reports and does not promote that report into a fresh executed-evidence verdict. Backend tests were pending at review request. Require their actual current-source results, including ordinary receipt/replacement and ownership coverage; the new omission/replay assertions are particularly relevant to the serializer compatibility decision. Preserve the full current regression and F1–F4 closure gates. Existing current-scope/replay tests remain relevant; no broadened authority was found in this diff.

The receipt field is a domain distinction, not a durable authorization token or a promise that a later command will succeed. Current roles, scope, revision and other business guards remain authoritative on execution. Pending browser work for ASSET_LOSS/TITLE_CORRECTION creation and C7 retained-unapproved-intent expiry are tracked separately in the earlier F1 notes; none is waived here.
