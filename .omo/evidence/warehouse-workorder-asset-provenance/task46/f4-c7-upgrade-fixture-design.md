Design-only C7 upgrade-fixture review, source HEAD `99ad992db31a6922ec1adf1957af81bf747426e6` with unrelated uncommitted C8 changes. C7 is not implemented; highest observed migration is 178.11. No product edits, builds, database probes or QA were run. Source hashes and inventory are in the adjacent JSON. This is a recommendation, not implementation acceptance or final F4 approval.

Recommendation: keep current C7 projections strict. Use a pinned pre-C7 application for old-schema HTTP setup, then start current code only after the complete forward migration. Do not introduce `missing clock schema => no expiry` into the current application's read path solely to preserve these fixtures.

The affected modern scope is exactly **two test classes**, each containing one test: `server/src/test/kotlin/com/duluin/ftth/inventory/WarehouseTransferDraftUpgradeIT.kt:15` explicitly boots schema 178.9, and `WarehouseCountDraftUpgradeIT.kt:15` boots 178.10. Their private `UpgradeClient` instances use current `FtthApplication` through `WarehousePostingFixture.kt:18`, seed valid commands, and call `database.migrate()` without replacing the application context. `WarehouseSchemaDatabase.kt:33` defaults that migration to latest. Consequently, a new C7 SQL dependency can fail during the initial HTTP setup, before the upgrade being tested.

The eight explicit old-target classes under `server/src/historicalTest` are different: seven run pinned historical application source with the complete current SQL chain, and one projection regression intentionally migrates only 175.21 to 175.22. Their current overlays are restricted to test classes, database/inventory helpers and migrations; C7 product readers are not overlaid. Exact targets/pins are in the JSON. Tests such as `WarehouseMigrationFulfillmentIT.kt:68` create schema 172 but do not pin Spring's target, so Spring migrates before current HTTP use. `WarehouseDiscoveryITUpgrade.kt:39` explicitly migrates before `postingContext(database)`. Old database construction alone is not an old-schema application boot.

Why the proposed fallback is insufficient: absent functions/tables mean the new database guards are absent too. Flyway defaults to latest, but an explicitly older target or disabled migration is not made safe by that default. No current application startup check enforcing a warehouse schema capability/version was found; `scripts/warehouse/preflight.sql:45` performs a separate operator probe. A blanket false/null expiry result would hide an unsupported boot. Adding a mandatory startup check would correctly reject that boot, including these two fixtures, so it would remove the reason for the fallback. Catching missing-relation/function SQL errors also aborts the PostgreSQL transaction; it is not a safe capability negotiation mechanism.

A precise fixture change can reuse existing infrastructure:

1. Run these two original regressions as a new pinned historical group, keeping the real HTTP setup, original command responses and all assertions. A concrete source candidate is `d6baf827ce9bcdae6222acc7c63b8fe64b89fa87`: both UpgradeIT files and `WarehouseTransferFixture.kt` / `WarehousePostingFixture.kt` are byte-identical to the reviewed current copies. It already contains both PUT implementations and has no C7 reader dependency. Keep its product and business-fixture source pinned; overlay the complete current migration chain and only explicit harness/test files, as `historical-upgrades.sh` already does. This source pin is not a claim that d6's overall CI passed; that run failed its server gate.
2. Preserve current-application upgrade proof by handing an owned database from the pinned setup to a fresh current process/context. Retain the database after old HTTP commands, close the old context/process and pool, apply the complete current migrations, then boot current code with its normal latest-schema requirements. A test-only child-process driver using a pinned classpath/JAR is preferable to mixing two `FtthApplication` versions in one classloader. The existing `WarehouseReceiptRestartProcess` / `WarehouseReceiptITRestart` provide bounded loopback, owned-database and non-owner-role checks to reuse. Record the bootstrap commit/artifact hash and current source hashes. No production compatibility flag or alternate clock implementation is necessary.
3. Retain the original private response/status, actor identity, idempotency keys and source fingerprints across that handoff. Assert byte-identical original authorized replay with current code, unchanged document/line/operation/command-identity snapshots and physical totals, correct migration inventory and repeat-migration no-op. Explicitly account for new sidecar activity/policy/expiry rows; they are not a reason to rewrite the old snapshots. Current checks must cover backfilled deadline/list/detail behavior, due-command rejection with the scheduler stopped, and successful permitted edits of still-live drafts. Use the previously reviewed owner-managed short tenant TTL and real database time for due cases; do not forge the clock or disable guards. Keep aged/future/nonfinite legacy-baseline cases separate from the old PUT happy path.

Moving two tests into the existing historical group is the smallest change that retains their present regression purpose. By itself it proves old application behavior against new SQL, **not** current C7 projection compatibility. The runner already states this limitation in its `verification.json`; the current-process handoff closes it. If implementing the handoff directly in these two fixtures, moving them is optional, but their pre-migration phase must still execute the pinned pre-C7 application rather than current code. SQL-only stock/operation/snapshot seeding would weaken their real-command provenance and replay proof; reserve owner SQL for bounded legacy-metadata backfill edge fixtures, without manufacturing business effects.

Concrete gate bookkeeping: `.github/workflows/warehouse.yml:74` and `:79` currently require these two exact classes in the focused modern reports. If moved, replace/remove those entries coherently, keep focused report class-set equality, add the historical group to `historical-upgrades.sh:112`, and increase its explicit seven-test expectation to nine. Preserve the separate one-test projection gate. Include the new group/handoff reports in encrypted evidence and verify actual XML identities/counts. Do not cap `database.migrate()` at 178.10/178.11, exclude C7 SQL from the overlay, skip these tests, or call an old-source result current-source evidence. The current historical migration assertions already derive the full packaged chain through `WarehouseMigrationInventory`.

For normal current boot, explicitly require successful C7 installation after Flyway and before traffic/scheduled mutations, without a profile/env opt-out; report a schema/setup failure on absent or partial clock capability. Validate the expected schema's successful migration/capability, not a cached global `to_regclass` boolean. If migrations are externally managed, validate the installed capability rather than requiring Flyway execution itself. Current eligible sources with missing activity must fail as an integrity error after authorization; posted/ineligible sources may legitimately have no live-draft clock. Keep authenticated original replay independent of current liveness while preserving current access checks, and keep every new-command deadline admission in SQL. A partial installation or missing row must never be treated as a live draft.

C7 explicitly requires idle drafts to expire without stock effects; immutable history and original authorized replay remain existing plan requirements. Pin choice, historical grouping, the process handoff and startup-check shape are reviewer implementation recommendations. Runtime acceptance of the eventual C7 implementation, migration and upgrade proofs remains pending. `GIT_MASTER=1 git diff --check` passed for the current validation worktree delta during this read-only review.
