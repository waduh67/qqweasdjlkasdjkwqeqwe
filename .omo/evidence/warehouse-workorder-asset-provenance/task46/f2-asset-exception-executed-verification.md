# F2 asset-exception executed-evidence verification

Status: **targeted executed evidence verified; final F2 pending**. Validation base `99ad992db31a6922ec1adf1957af81bf747426e6` plus dirty C8 changes. Reviewer used read-only XML/log/source inspection; no QA or product changes. Exact hashes and verification time are in the accompanying JSON.

- **Server R2: 87 passed / 4 suites** (9 context, 73 ownership, 2 loss, 3 module). **R3: 13 passed / 2 suites** (10 context, 3 module). Actual testcase and declared totals agree with saved counts; no failures/errors/skips. Exact report hashes match. Both logs show executed source/test Kotlin compilation, an executed test task and successful build completion. R2 log/runner hashes and safe proof also match.
- All **2,602 R3 server/src inputs match current source**, with no additions/deletions. R2 to R3 changes exactly `InventoryAssetTitleService.kt` and `CustomerAssetExceptionContextIT.kt`: the legacy ownership reader now uses the scoped late customer lock, and the test adds ownership positive/race coverage. **Keep the broader R2 87 and current R3 13 separate**. The 73-case ownership suite was not rerun after the production change; this is neither an 88-case current regression nor 100 distinct cases. Build configuration and external modules/toolchain are outside the input-map scope.
- Ownership XML has **73 testcase invocations and 65 distinct display strings**. Eight strings repeat because separate inherited parameterized methods use the same scope/authority-field values. Source inheritance explains these collisions; deduplicating display strings would undercount execution.
- **Web R2: 18 passed** (11 exception, 7 panel), with exact report/log hashes. All **520 src inputs** still match. Of **546 src+e2e files**, only `e2e/warehouse/asset-exception-journey.ts` changed afterward. The root's recorded sequential fail-fast runner (session 63261, terminal/exec exit 0), plus independently checked log continuity and successful build completion, supports passing E2E typecheck, lint and production build. The log has **101 located lint warning diagnostics**. Browser navigation was only typechecked at its R2 version; the changed helper still requires current validation.

Limited source recheck confirms the earlier customer-area race recommendation is implemented: `AssetHandoverCustomerAdapter.lockForException` checks current fenced scope under the late customer row lock, preserving the existing owner lock order. Current context and ownership reads use it; title/loss proposals also reauthorize before fresh/replay results. R3 covers LOAN/SALE ownership positives and a deterministic ownership area-change race. Workorder retains named-WO/evidence authority and emits signature ID/name/receipt time without storage references.

Raw runtime evidence remains private. These are focused server and mocked-HTTP UI results. Completed real-browser evidence, full current regression, later affected-source validation, and **C7 retained-draft idle expiry** remain required. **No final F2 approval.**
