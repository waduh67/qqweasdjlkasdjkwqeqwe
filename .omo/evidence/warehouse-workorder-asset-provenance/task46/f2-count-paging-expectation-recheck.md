# F2 COUNT paging and test-expectation recheck

Status: **No additional material issue found in the reviewed corrections. Corrected execution and final F2 gates remain pending.** Source-only review of the uncommitted validation patch on `6c6195aecbb785dc5094d382845665e35b8e73ef`; no reviewer QA.

- The positive variance case now receives a new BULK/EA SKU with 100 units, puts it away in a BIN, transfers all 100 EA into the target warehouse, edits the count to that position, and observes 80 EA. This matches `WarehouseCountVarianceStore.legs`, which explicitly requires BULK/EA for an adjustment. The unchanged 100000 MM branch remains. Source revision 4, independent approval/replay, round revision 2, exact final quantity and one variance movement assertions are retained. Production guards were not relaxed to accommodate the test.
- The first raw transfer edit does not increment revision; expecting SQLSTATE 40001 matches `warehouse_revision_guard`. The separate next-revision corruption, cleared binding, immutable operation and posted-edit assertions still require 23514. The correction narrows one expectation without weakening the other checks.
- COUNT paging slices only the rendered rows. The complete 1–100 row array stays in parent state; hydration reads every saved assignment, keyed updates/removal operate on that complete array, validation checks every row and duplicate balance ID, and confirmation/PUT map the complete array. Page changes do not rewrite selection or eligibility state. Removal clamps the displayed page; adding selects the page containing the new row. No row loss or off-page eligibility bypass was found.
- Missing positions and `counterValid === false` still block preparation even on another page. Current server eligibility is rechecked for every submitted assignment under the authority fence; the browser's retained selection is not treated as authorization.
- The revised 100-row UI test checks row 1, switches to page 4 and checks row 100, then verifies one exact PUT containing all 100 assignments and the saved revision. It also checks there is no pre-confirmation write or stock/lot/user lookup. The separate missing-selection/conflict test retains explicit reselection and reload requirements. The source assertions support the paging behavior without requiring all 100 editor rows in the DOM simultaneously.

`git diff --check` passed. The coordinator reported the earlier focused run as 67 tests/19 suites with 65 passes and two expectation failures, including successful upgrade cases. That run is not recorded here as a complete pass, and this reviewer has not inspected completed corrected-run or web-r4 reports. The companion manifest records hashes for the specific sources inspected in this follow-up.
