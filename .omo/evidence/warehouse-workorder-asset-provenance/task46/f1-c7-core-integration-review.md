# F1 bounded C7 core integration review

Independent read-only source review of uncommitted validation-worktree changes on HEAD `1d14f4b9aebcd1c1a9e8c84041259ff56a422005`. Candidate `V178_12__warehouse_idle_draft_expiry.sql` hashes to the previously reviewed `add301336feaf41c740f5d6026206500aed07c6bae7c20f168d7aa4ae11adca3`. The parent reports a rollback schema probe, but this reviewer did not execute or validate runtime QA, and the candidate had not yet been applied through Flyway when this task was requested. Exact source hashes are in `f1-c7-core-integration-source.json`.

Scope: WarehouseDraftLifetimeStore/ExpiryService, receipt canonical-save integration, approval/posting expiry paths, and directly changed ordinary current readers. UI, specialized proposal projections and broader runtime/upgrade gates remain known pending work. This is not final F1 or C7 implementation acceptance.

## Concrete remaining findings

**C7-core-1 — Material plan reads can disagree with their own expiry metadata.** `MaterialPlanningStore.current/get/history` select `warehouse_plan_current_state(...)` in one SQL call; `readHistory` then calls `lifetime.plan(plan.id)` in another call and returns the earlier state unchanged. A deadline crossing between those calls produces `state="DRAFT"` with non-null `draftExpiry`. This needs correction before accepting the changed current readers. Derive effective state and metadata from the same expiry observation, or return both from one coherent source query. Preserve submitted state and maximum plan revision. Receipt/count currentView already derive state and metadata together.

Evidence: `server/src/main/kotlin/com/duluin/ftth/inventory/adapter/outbound/persistence/MaterialPlanningStore.kt:13`, `:23`, `:32`, `:44`.

**C7-core-2 — Main work-order material summary loses expired-plan state.** `InventoryMaterialService.summary` obtains the new MaterialPlanHistory with expiry, but discards history state/expiry when constructing MaterialSummary. With no submitted demand, an expired latest plan still gives `demandState=DRAFT`, and the summary has no separate current plan state/expiry signal. History/get do expose EXPIRED. Include this ordinary summary in the remaining projection work; explicit current plan state/expiry avoids conflating an expired plan with a demand that has not been created. Do not change the immutable plan snapshot, lower the latest revision, or fall back to an older plan.

Evidence: `server/src/main/kotlin/com/duluin/ftth/inventory/application/service/InventoryMaterialService.kt:22`, `:49`; `server/src/main/kotlin/com/duluin/ftth/inventory/InventoryMaterialApi.kt:88`. This is a concrete integration obligation, not a claim that the parent's known pending UI/projection work is complete.

## Core behavior supported by source

- The worker's transactional entry obtains cutover CONTROL_PLANE, exclusive tenant authority, a source row, then pending approvals ordered by ID. It inserts expiry and terminates pending approvals in one transaction, matching the deferred SQL invariant. Each transaction handles one source; the scheduler caps each tenant pass at 25 and logs tenant/code only. The authority fence blocks ordinary concurrent commands before owner/source locks; expiry does not mutate owner, stock, assignment or policy epochs.
- dueDocument/duePlan select with SKIP LOCKED; expireDocument/expirePlan invoke the database function, which locks and rechecks eligibility, current activity and actual time. An earlier candidate observation cannot override a winning fresh save. Source selection remains limited to retained DRAFT; COUNTING/SUBMITTED counts and posted/in-transit effects are not expired.
- Fresh receipt PUT/transition, transfer mutation, count update/start and material-plan submission perform liveness checks after existing authorization and successful replay lookup. SQL child/header guards still enforce expiry after waits. Authorized old response replay remains separate from current-state overlays; no replay appends activity.
- `WarehouseReceiptService` inserts the original operation, then its existing intake identity, then the new canonical-request seal using canonical.json. The operation trigger captures activity at the save boundary; the deferred structural check sees both immutable identity records at commit. The original intake identity format and response view remain unchanged. Optional current expiry DTO fields use NON_NULL where those types are reused in original command snapshots.
- Approval request checks source liveness after source authorization and original request replay. Decision paths detect due sources; final decision/posting expiry failures use ApprovalPostingStopped to roll back and enter the existing recovery transaction. WarehousePostingPersistence now catches expiry at documents.advance as well as movement-header insertion after stock waits. Recovery rechecks current actor/source scope before retaining the terminal response.
- Approval query/source/detail paths use current expiry to block requesting/deciding/rework and expose current EXPIRED without rewriting immutable approval source snapshots. Terminal pending-approval cleanup occurs under the already acquired source→approval order. Generic approval status updates have not gained a late source lock.
- SQL constraint mapping distinguishes the exact `warehouse_draft_expired_ck` PostgreSQL constraint as typed DRAFT_EXPIRED. Other integrity failures remain separate. Source-current getters and recorded command histories still use their appropriate raw/preserved state where original response binding needs it.
- Material-plan replacement uses the latest plan revision regardless of expiry and creates a new immutable identity/clock. It does not revive the old plan or resurrect an earlier revision. Submitted plan and demand lifecycles remain separate.

No additional concrete security or lock-order blocker was found in the requested worker/receipt/approval/posting paths during this bounded source review. The two material-read findings, known remaining integration work and executed non-owner save/replay/expiry/approval-race/upgrade tests are still prerequisites. Test source presence and parent-reported execution are not independent executed evidence from this reviewer.

## Same-task source follow-up

The parent corrected both material-read findings while this review was active. Rechecked source now makes `readHistory` derive EXPIRED from the same `lifetime.plan` observation attached as metadata. MaterialSummary adds optional NON_NULL `planState` and `draftExpiry`, populated from the latest history while leaving demand state separate. C7-core-1 and C7-core-2 are therefore addressed in source; no remaining finding from this bounded review is asserted against these updated files. Their replacement hashes are recorded separately in the manifest so the original finding inputs remain traceable.

The summary is also serialized for new SUBMIT/RESERVE/RELEASE responses; those commands retain the exact generated body, and replay returns stored original_body rather than recomputing the new summary. The new metadata does not rewrite old command bodies or immutable plan snapshots. Required integration, UI, specialized projections and executed runtime gates remain pending. The parent's active test run was not run or independently verified by this reviewer.
