# F4 — authorized host VPN preparation/install source review R1

Source-only review of the two private helpers, compared with application commit `1306b65c34167b2d48f4817ce72990fed3b85aa3`. No helper, shell installer, service, network request, SSH, Docker or QA was executed by this reviewer. Python sources were parsed without importing or executing them. The current full code CI remains pending and untouched. This is not F4 acceptance or permission to activate/install.

**Two corrections are required before this host installer is ready.**

1. **High — clean-host installation aborts in the unchanged generic application installer.** `server/src/main/resources/vpn/install.sh.template:273` runs a top-level legacy-DNAT filtering pipeline under `set -euo pipefail`. An empty match makes `grep` return 1, so the installer exits before enabling/restarting its services. Preparation explicitly rejects the matching legacy tunnel rules, making an empty cleanup the intended fresh-host case. This is a pre-existing generic installer defect, outside the warehouse change; the existing `VpnInstallScriptTest` checks generated text and does not execute this path. It is a source finding, not a newly executed failure.

   The parent's proposed host-only correction is appropriate: retain the exact downloaded original, check `iptables -t nat -S PREROUTING` separately, use an empty-success filter, and preserve the existing scoped deletion behavior. A listing error must still abort; unrelated rules must never be deleted. Preserve exact original/patched hashes and diff, separately approve the patched bytes, and authenticate stub execution for empty, matching, unrelated and listing-error cases. Do not disable `errexit` globally or seed a dummy legacy rule. This can clear this particular host setup defect without claiming that the generic 1306 installer was fixed or requiring an image rebuild.

2. **Medium — overwrite/conflict checks are incomplete and occur too early or too late.** `install-production-vpn.py:34` checks only the server directory before running an installer that overwrites its sync units and `/etc/sysctl.d/99-ftth-vpn.conf`. Preparation's route/rule/unit checks are not repeated immediately before mutation. The firewall helper, unit and drop-in checks occur after installation. Neither helper currently rejects a pre-existing 1194 listener or legacy server configuration. Thus a conflicting owned path or stale preparation can be discovered only after files/rules have changed. Repeat all applicable owned-path, server-service/listener, route/subnet and rule checks immediately before running the approved installer; check the sysctl path and all added helper/unit/drop-in paths first. Treat an absent server directory as fresh, while rejecting a nonempty directory. No actual overwrite or conflict was observed.

The intended design otherwise stays within the authorized hub setup: a real platform-admin session creates or reuses the exact Azure FTTH hub, saves its issued node token privately, downloads its actual installer, checks syntax/certificate chain/key pairing, and requires a separately hash-bound install decision. It creates no peer. The host boot unit restores only the same three tunnel-subnet rules used by the shipped installer, orders itself after Docker and before OpenVPN, and avoids restoring a saved global Docker ruleset. HTTPS callback markers plus a rejected invalid credential are bounded callback checks; they do not prove a physical peer, forwarding traffic, or a reboot.

Retain those evidence limits after correction. Recheck OpenVPN/timer/listener readiness after the firewall idempotence restart as well as before it. The service's exit status alone is not proof of callback application: the shipped sync script intentionally treats unavailable callbacks as a successful no-op, so retain the separate authenticated callback assertions. Runtime output, node tokens, downloaded installer/certificates/private keys and curl bodies remain private.

The actual rendered installer, any patch, its synthetic cases, host preconditions, installation, callback results and boot behavior are still unverified. Source hashes and exact scope are in the companion JSON.
