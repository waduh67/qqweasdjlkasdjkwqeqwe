Bounded source recheck of the CI-only protocol patch on `9d540107fc2fd47a3b02312371c9e65df75acb83`. Workflow SHA-256: `9477b01cf3a9fa03ac8c1eab27ca3ee9fb38e93604c552b7f9e0e456950304c1`. Reproduction-document SHA-256: `3f7d50dd19943c6e509b46f85a9dccc0dd25a31d499eb927eddb5b5a370fd3fc`.

The cache/execution finding is fixed in source. The `contract`, `snmp` and `collector` test invocation includes `--rerun-tasks --no-build-cache` in both the workflow and documented reproduction. Removing only that flag pair reproduces the previously reviewed workflow/command hashes. The other changed documentation clarifies the execution order and forced rerun; reversing just those wording changes reproduces its previous hash. The other eleven reviewed files are byte-identical. No other gate change was introduced.

All three module Test tasks remain mandatory in the required server job. Individual verification requires nonzero successful testcase results, rejects missing/failing/error/skipped reports and exits nonzero. Raw protocol logs/XML use the existing authenticated encrypted archive; public receipts contain only allowed suite/count/hash metadata. Existing diagnostics survive an early protocol failure. Failure still blocks acceptance, the reusable workflow, publication and deployment. Offline GPON claims do not imply physical device certification.

No remaining concrete issue was found in this bounded patch. YAML structure and `git diff --check` passed as source checks. No local QA, build, protocol test or product edit was performed. Root/F3's planned workflow tests and actionlint are not claimed as completed here. Actual current module reports and final F4 acceptance remain pending.
