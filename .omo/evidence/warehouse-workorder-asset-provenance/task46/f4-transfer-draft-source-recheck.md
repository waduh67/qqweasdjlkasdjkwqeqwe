# F4 transfer draft source recheck

Source-only recheck of the uncommitted validation worktree at `149ecfb23d65de96ff661444cdbfa7da473ab401`. No remaining critical/high or V178.10 migration blocker was found in this bounded review. This is not final F4 approval: Java/web/SQL execution and the complete current release gates remain pending. No product files were edited and no QA was executed by this reviewer.

The four earlier findings are addressed: the DRAFT guard requires four nonnull bindings and exactly the next revision; the latest command reason matches the document; GET locks topology before reading and details join the exact snapshot line IDs; inactive receivers remain visible and repairable in DRAFT while a new binding and dispatch require an eligible receiver. See `V178_10__warehouse_transfer_draft_revision.sql:11`, `WarehouseTransferService.kt:99`, `WarehouseTransferQuery.kt:73`, and `WarehouseTransferAccess.kt:40`.

A further medium replay defect found during this pass was corrected before this note was finalized. Create/PUT now check current actor, request/current location scope, canonical hash, resource and cutover binding before returning an existing response, and apply receiver eligibility only to a genuinely new command. The disabled-receiver test preserves both original create and PUT status/body after repair and after dispatch. See `WarehouseTransferService.kt:26`, `:56`, `:148` and `WarehouseTransferDraftIT.kt:155`.

V178.10 keeps immutable former draft command bindings while requiring current/latest and all posting bindings to match the document. Sender identity remains fixed. A create/update snapshot cannot follow a posting command; draft operations cannot have stock movements. Existing discrepancy, independent resolution approval, quantity conservation and quarantine guards remain in the composed assertion. Store replacement is fenced and revision-conditional, replaces only DRAFT lines, records the immutable next command, and performs no stock posting. All 364 prior migration files are byte-identical to validation HEAD; the only new migration is V178.10, SHA-256 `d82891a8941a294bab114069297e931cf8ad6cd0cc7d8a8a7e2f06e736bd4ce2`.

The controlled details test holds the target document row, proves PUT is blocked behind it while holding topology, then proves the viewer waits behind that writer before release; the final viewer must receive404 without hidden source/identity/lot data. Concurrent PUT/PUT and PUT/dispatch tests require one200 and one409 and check the winning revision/stock outcome. The separate upgrade fixture boots current compatible create/dispatch code against an owned178.9 database, snapshots durable document/line/operation/identity/movement rows, migrates to178.10, compares those snapshots and replays existing commands. This is a schema upgrade test with current compatible code, not proof of an older binary. The fixture explicitly targets its owned database and closes application context before dropping it. These are source-reviewed assertions, not executed results.

The web edit resolves at most100 saved positions plus three locations through scoped APIs; absent old position IDs require selection again. It retains exact base quantities, requires an active replacement receiver and uses the stored expected revision. History checks each prior snapshot's own locations before pagination. Documentation marks migration runtime validation pending, advances preflight expectations to178.10, and does not claim native runtime or release from compilation.

`GIT_MASTER=1 git diff --check` passes for the current validation delta. Reviewed file hashes are in `f4-transfer-draft-source-recheck.json`. Existing whole-feature immutable whitespace exceptions, broader F1 expiry audit and final current runtime/preflight requirements remain outside this checkpoint's approval.
