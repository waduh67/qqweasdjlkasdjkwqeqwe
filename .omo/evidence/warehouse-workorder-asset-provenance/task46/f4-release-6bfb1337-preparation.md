Source/release preparation for `6bfb13374db245e0f4d192c94ade42fdb07cd613`; this is not final F4 approval.

CI snapshot at 18:17 UTC on 2026-09-26: old `1d14f4b9` run 36252487886 still executes its full server/fresh/historical step, started at 15:56:29 UTC. Compile, focused and thirteen nonserver jobs passed; no server artifact exists. Current `6bfb1337` run 36260966532 is pending with zero jobs. The authenticated 1d nonserver proof is preserved byte-for-byte. There is no aggregate current CI pass or reliable completion ETA.

The 9d→6b diff changes only the protocol workflow, documentation and review records. All other tracked product, build, fixture and runner inputs are identical. The 3693-file current source hash inventory includes contract/SNMP/collector and build inputs. Local protocol receipts record contract 2, SNMP 25 and collector 170 cases. All 130 recorded inputs match current Git; the parent's F1 reviewer authenticated the raw evidence. F4 performed source binding only. The committed five-test workflow/actionlint proof also matches current source. Neither receipt is relabelled as completed CI.

There are 367 unique packaged migrations, highest 178.12 and next 178.13. Every migration path and byte from e178 (364), 1d (366) and 9d (367) is preserved. Applied V178.12 matches its manifest SHA-256: `add301336feaf41c740f5d6026206500aed07c6bae7c20f168d7aa4ae11adca3`. Current startup requires the clock tables, FORCE RLS, functions and enabled triggers. Unsupported old schemas fail startup; pinned old-process fixtures remain test-scoped. Runbook recovery uses a new forward migration or verified coordinated restore, without direct ledger changes or disabled validation. Protected-history tenants use Suspend; empty tenant deletion remains supported.

The required server job forces contract/SNMP/collector tests without cached task results and verifies nonzero reports per module. Raw logs/XML/bootstrap replies remain encrypted; public receipts contain bounded counts/hashes. Acceptance requires every server, web, shared, browser, legacy, native and image gate. Failure, skip, cancellation or a missing gate blocks publication. Publication consumes the exact tested archives and verifies image/config/registry identity; deployment uses immutable registry digests and a checked Compose hash. The expanded current workflow requires 16 jobs and 18 browser spec/project pairs. This shape is not evidence of an executed browser count.

Current docs match 178.12, retain legacy identities, distinguish loan/sale and customer-owned RMA without a BYOD or accounting path, and retain offline-only GPON and compile-only native limits. No new scope or release-gate defect was found in this preparation. It does not replace detailed F1/F2 review of C7/C8. Source, images and preflight from 1d cannot validate the newer expiry implementation.

Tracked source inputs are unchanged in the validation worktree; concurrent edits are review records only. The 9d→6b `GIT_MASTER=1 git diff --check` passes. The whole feature diff still reports five recorded EOF hygiene cases and two immutable V175_132 whitespace lines (264, 450). These are nonblocking hygiene exceptions; applied checksum preservation takes priority. No files were changed to clear them.

Final F4 remains pending current full CI, authenticated current image/server/historical artifacts and executed current runbook preflight against an isolated upgraded fixture under the QA lease. Old positive/negative preflight and customer/ONU counts remain bound to their tested source/schema. No local QA, SQL probe, build, Docker execution, publication or deployment was performed.
