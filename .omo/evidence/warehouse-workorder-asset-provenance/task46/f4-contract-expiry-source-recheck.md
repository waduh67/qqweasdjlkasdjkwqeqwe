The correction changes exactly four expected strings in `WarehouseContractTest`: EXPIRED in Receipt/Transfer/Count and DRAFT_EXPIRED in the 409 error group. Exact ordered enum membership, wire serialization round-trips and HTTP-status assertions remain intact. The patched bytes equal only those four replacements against tested 6b; all other 3,692 source/build/fixture/config inputs are unchanged. No product or workflow change is included.

Independently parsed the existing private local reports: 12 WarehouseContractTest and three ModularityTests cases pass, with zero failures/errors/skips. Both XML hashes match the safe receipt, the recorded exit file is zero, before/after inputs agree, and all 3,693 recorded source hashes match the reviewed working inputs. No new local QA was executed. The evidence-only parent c122 does not repair its already-running CI snapshot; a current complete CI remains required.
