# F2 local COUNT/transfer executed-evidence verification

Status: **combined focused evidence verified; final F2 approval pending**. Read-only inspection only; the reviewer did not run QA or change product files. Validation base is `6c6195aecbb785dc5094d382845665e35b8e73ef`, with uncommitted COUNT work. Exact proof/report/log/source hashes and verification timestamp are in the accompanying JSON.

- Parsed every private XML report independently, checking testcase counts, failure/error/skip children, declared suite counts, duplicate identities, class attribution and exact report hashes. R1 is **67 cases / 19 suites, 65 passed and 2 failed**, with a failed-build completion marker. R2 is **14 cases / 2 suites, all passed**, with a successful-build completion marker. Both log hashes match the root proofs. The R1 proof hash chain also matches.
- Compared all **2,601 recorded server/src inputs** against every current file in that tree: no additions/deletions; **2,599 unchanged**, and only `WarehouseCountDraftIT.kt` / `WarehouseTransferDraftIT.kt` differ. Their current hashes match R2. Gradle/toolchain configuration is outside this source-map scope.
- Reconstructed the root composition exactly: **53 passing cases from 17 unchanged R1 suites + 14 passing cases from the two complete R2 classes = 67 passed / 19 suites**. COUNT draft/upgrade contributes **9** and transfer draft/upgrade **7**. The transfer case IDs match; COUNT's positive variance parameter changes `80000` to `80`, consistent with the reviewed BULK/EA fixture correction. **This is a combined focused result, not an unfiltered run, and does not turn R1 into a passing run.**
- Independently parsed the web report: **9 cases passed**, no failures/errors/skips or duplicate cases, with exact XML/log hashes and all **six recorded web source hashes** matching the current tree. The 100-row case passed in **1.845382079 seconds**. The log contains the warehouse E2E TypeScript check command, **99 located lint warning diagnostics**, the production build command and successful build completion. The updated root proof preserves the exact four sequential commands under `set -e`, aggregate terminal marker `COUNT_WEB_CHECK_EXIT 0`, and tool exit `0` from session 5469. That fail-fast runner receipt plus the independently inspected log continuity/build completion supports all four commands returning zero. The receipt was recorded by root after execution; the standalone log does not expose per-step exit codes.

Raw XML, system-out, failure bodies and logs remain private. This note contains only safe counts, source identifiers and verification facts. Real browser execution, complete current regression, any later affected-source reruns and remaining feature work (including retained-draft idle expiry) are still required before final F2.
