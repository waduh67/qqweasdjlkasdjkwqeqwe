# F4 source-bound server artifact recheck: 149ecfb2

Independently verified the completed server evidence for `149ecfb23d65de96ff661444cdbfa7da473ab401` in CI run36149519466. This is a pass at that tested commit only, not final F4 approval or validation of later transfer changes/V178.10. No local Java, Gradle, database, Docker, browser or other QA was run.

Fresh GitHub metadata confirms the exact commit and all15 jobs completed successfully. The downloaded artifact is10879075502. Its ZIP SHA-256 is `1db0e8d100ccc64c5141dc9581e92bb4bbc0c85a3a0ed3a91b048daf5d773ea7` and encrypted archive SHA-256 is `8f54fc09213a2f75bc8cd8d0303913b0b3480322a2f041ea6d463b8eeb8e1239`; both match GitHub/download/encryption metadata. All six published ZIP members match the local extracted artifact. All670 plaintext tar members match the private raw extraction, with no missing/extra members. This reviewer independently authenticated age decryption with the existing authorized evidence identity and matched its plaintext SHA-256 to the reviewed tar archive. Plaintext was consumed privately in memory; no key or raw evidence was printed.

Direct XML parsing verified3,792 successful modern tests in612 suites,266 focused tests in34 suites, seven historical tests and one projection upgrade test. All654 XML files have the stated number of actual testcase elements and zero failures, errors or skips; their SHA-256 values match the published receipts. The focused class set exactly matches the tested workflow, and all34 classes also appear in the complete modern run. The workflow removes focused XML before its unfiltered full server command. Private logs match the root receipt and contain successful completion markers without BUILD FAILED.

The legacy read scope regression has two passing tests, legacy reservation projection has three, and ModularityTests has three, each present in both the focused and complete modern results. These are included in the totals above.

Every one of376 distinct historical runner/test/helper/migration input hashes matches the Git blob at149ecfb2. Each historical group contains the complete364-file migration inventory through178.9. Pinned migration files also match each historical application commit. The projection regression and V175.22 migration hashes match149ecfb2, and its actual single XML report matches its receipt. The detailed reviewed hashes and safe counts are in `f4-ci-149ecfb2-server-recheck.json`; no XML system output, HTTP responses, private fixture rows or credentials are copied into this note.

This agrees with the root's `ci-149ecfb2-complete-server-verification.json`. Later commit6c6195ae differs in39 files, including V178.10, so this evidence does not establish that later source passes. The interrupted local R7 is not counted as a completed pass. Nonserver artifact contents, new runtime results, exact later image identity and the required final preflight remain separate checks; successful native compilation is not a native runtime/release claim.
