# F1 targeted C7 revised SQL recheck

Read-only review of the private, unapplied original-workspace `.omo/runtime/warehouse-draft-expiry.sql`. Initial revision read: SHA-256 `712566b22148e09a1a1f927f48e574c864c69027ec04fe61fbaecc655dcc2789` (531 lines). The parent corrected the cost comparison during this review; the next revision read has SHA-256 `ca5527c05bf68614b7d2da4e5b2b90dc6c27609532fb62db89ae61f9584ef3c3`. Product references are validation-worktree HEAD `99ad992db31a6922ec1adf1957af81bf747426e6` plus existing C8 changes.

No product/test/plan edits or runtime QA were performed. This is not migration execution evidence or final F1. Store integration, worker, read/UI overlays, errors and tests remain known pending work.

## Recheck of earlier findings

- R2 receipt-evidence insertion: addressed in source by a BEFORE INSERT `document_id` liveness hook.
- R3 concurrent new-draft backfill window: addressed for source creation and normal accepted-save writers by the opening SHARE ROW EXCLUSIVE locks on material plan, document and operation tables, before baseline capture and trigger installation.
- R4 effected DRAFT sources: addressed in source by movement/effect liveness hooks, a deferred structural inverse forbidding an APPLIED movement or approval effect for retained DRAFT, integrity rejection during backfill, and the same assertion before expiry/current due projection. There is no new wall-clock test in historical deferred integrity validation. Successful normal posting leaves DRAFT before inserting effects; the original header transition remains the late admission point after stock locks.
- Expiry timestamp meaning: the current-state helper now returns the referenced activity deadline before and after terminal persistence. Observed terminal-record time remains separately available.
- Pending approvals: the new deferred expiry invariant requires pending requests to terminate in the same transaction. Existing approval lifecycle prevents reopening terminal requests; new approval insertion locks/checks the source. The planned worker must perform that termination before commit. The new invariant does not add source locking to generic approval status updates.
- R1 receipt save: substantially addressed by operation-linked protected activity containing the save-boundary snapshot, a separate immutable canonical-request row, and a deferred seal over original canonical hash, original intake identity, response and captured document/line content. Header changes later in the same transaction do not bypass that captured-state comparison. Receipt identity format and original response bytes stay intact. Remaining field omissions below prevent calling the structural seal complete.

## Findings in the revised receipt seal

**Valid input regression, corrected during review:** the first revised sketch compared the whole input cost object with normalized intake cost (excluding its basis). `StockQuantity.parseBase` permits leading zero digits, while `ReceiptDraftValidation` normalizes `totalMinor`. Thus accepted `totalMinor="007"` becomes intake `"7"`, incorrectly failing a valid PUT at deferred commit. The parent changed the comparison to numeric totalMinor and basis, exact currency and cost nullness; the correction is present in the later revision read. Original canonical bytes/hash remain unchanged. Quantity comparison is already numeric; conversion is intentionally retained as its original ReceiptPackageInput in intake.

**Remaining structural binding gap:** at the later revision read, activity captures only `inventory_receipt_intake.snapshot`, not the relational intake source/inspection location columns. V174.12 validates their FKs and draft parent but does not bind those columns to snapshot; receipt candidate filtering uses the columns. Capture the full intake row and bind its identity, tenant, source_location_id and inspection_location_id to the captured intake/request.

The captured line comparison also omits fields written/defaulted by `WarehouseReceiptPersistence.saveDraft`: inspection_required_snapshot; conversion_numerator, conversion_denominator and package_quantity; fresh-draft null lot_id/source_line_id; zero accepted_base/rejected_base/missing_base; and default continuous_cut. Existing generic constraints do not make these equal the accepted save. For conversion, SERIAL lines deliberately store SQL NULLs; other tracking types store the numerically parsed input conversion. Nullable custodian_kind, condition and legal_owner use `<>` rather than `IS DISTINCT FROM`, so SQL NULL can evade those predicates when all other comparisons are false. Bind these fields explicitly and use null-safe comparisons.

Evidence: `server/src/main/kotlin/com/duluin/ftth/inventory/adapter/outbound/persistence/WarehouseReceiptPersistence.kt:22`, `:37`, `:87`; `server/src/main/kotlin/com/duluin/ftth/inventory/application/service/ReceiptDraftValidation.kt:34`; `server/src/main/kotlin/com/duluin/ftth/inventory/domain/model/StockQuantity.kt:65`; `server/src/main/kotlin/com/duluin/ftth/inventory/application/port/inbound/WarehouseReceiptModels.kt:45`; `server/src/main/resources/db/migration/V174__warehouse_documents_and_balances.sql:28`; `server/src/main/resources/db/migration/V174_12__warehouse_receipt_intake.sql:39`.

The response shape matches the current ReceiptView constructor: draftEditability is null in original command replies and explicitly omitted by NON_NULL. It belongs in current reads only. Cost is redacted in the stored draft response. Do not insert new current-state/editability fields into the original receipt used by this seal.

After the remaining field binding correction, recheck the final sketch hash and execute the already planned non-owner raw-write, replay, save, expiry/approval race and migration gates. None was executed by this reviewer.
