Bounded task 46 expiry review, recorded 2026-09-26T17:27:22.325656+00:00.

Reviewed workspace: /home/fajar/ftth/qqweasdjlkasdjkwqeqwe/.omo/runtime/warehouse-regression-validation
HEAD: a85b3fda952e16a38913711f03ee8c8819382cc1. Exact reviewed files and SHA-256 values are in bounded-expiry-backfill-r2-sources.json; HEAD alone does not identify dirty files.

No additional actionable defect was found in the reviewed source. This is a bounded source review and existing-evidence authentication, not final F2 approval.

The revised WarehouseDraftExpiryUpgradeIT installs owner-created BEFORE INSERT fixture triggers only in an isolated warehouse_fixture database at schema 178.11. The document trigger changes only TRANSFER updated_at; the plan trigger changes new plan updated_at. The historical seed creates six transfers and six material plans through the pinned pre-expiry HTTP application, in sequential ordinal order. This exercises old, future, positive infinity, negative infinity, finite recent and current timestamps. Existing immutable guards are not disabled or replaced. The fixture triggers/function are removed before V178.12 migration. There is no production clock override; protected activity and deadline evaluation still use database time. The former post-creation UPDATE of sealed plans is gone.

The migration barrier holds ROW EXCLUSIVE on inventory_document and observes an ungranted ShareRowExclusiveLock in that isolated database while the migration future remains pending. A database timestamp is sampled immediately before releasing the blocking transaction; both activity families must have a baseline at or after it. The assertion therefore proves the migration relation-lock conflict and baseline ordering. Its explicit lock models what INSERT/UPDATE acquire; it does not execute an in-flight legacy HTTP write or prove an additional concurrently inserted row is included.

Every seeded record must have exactly one queried LEGACY_BASELINE activity. Deadlines are checked against the migration baseline and seven-day upper bound: old and nonfinite dates clamp to the baseline; future dates clamp to baseline plus seven days; recent/current finite dates retain source.updated_at plus seven days. Source fingerprints span documents, lines, receipt intake, operation responses, command identities, material plans/lines/submissions, movements and balances. They must match after migration and again after current readers, original HTTP replay and rejected due dispatch/submit commands. A repeated migrate call must execute no migration. Fingerprints intentionally exclude newly created clock side tables.

WarehouseDraftExpiryScopeIT proves that a scoped caller can see an expired receipt before revocation, then receives 404 for detail and original-key replay after inspection-scope revocation. Its EXPIRED list is empty, including totalElements, both before and after worker materialization. Clock activity must remain unchanged and an authorized admin retains access. This closes the earlier explicit scope-after-expiry coverage gap for receipts.

The historical transfer seed captures the original dispatched response text as shippedBody. WarehouseTransferDraftUpgradeIT compares current replay text directly against it in addition to the parsed replay comparison. The existing transferAction helper checks the dispatched replay success status. This extends the wire-body comparison without changing product behavior.

Previously reviewed changes are now stronger: the browser test asserts exactly the original line ID/SKU/quantity/lot and zero accepted, rejected and putaway totals with empty pieces, closing the empty-lines every() false positive. The specialized repair test now captures repair/consumption state and verifies a fresh same-serial receive; its exact tested source is covered by the specialized R3 evidence below.

Existing evidence authenticated by read-only parsing and hashing: specialized R3 has six XML files whose hashes and testcase totals match the recorded manifest, with 18 tests and zero failures/errors/skips. Browser R1 contains exactly two successful single attempts, desktop and mobile, with zero unexpected/flaky/skipped cases. Per-file proof hashes, source-capture comparisons and report bounds are in bounded-expiry-evidence-authentication.json. The R3 and R1 captures do not include subsequent changes named there. In particular, browser R1 predates the screenshot scroll change and later QA selector update.

All 17 reviewed files represented in the active backfill R2 input manifest match their captured hashes. This does not authenticate a completed R2 runner or cleanup result. The parent-reported DeleteTenant fixture/GUC issue is outside this source review and is not classified here as a product defect. No Java, Docker, browser QA, database mutation, product edit or test edit was performed by this reviewer. Only this private review directory was written. Runner/cleanup, typecheck/build and hardware conclusions are outside the independently verified evidence above.
