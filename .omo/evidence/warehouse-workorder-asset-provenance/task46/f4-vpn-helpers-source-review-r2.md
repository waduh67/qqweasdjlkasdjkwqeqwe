# F4 — VPN helper source corrections and fragment evidence R2

**The two R1 source findings are closed for the reviewed host-helper design.** This approves the bounded source corrections, with the existing activation, actual-installer review and separate hash-approval gates still required. It is not permission to install, a runtime PASS, or final F4 acceptance. No helper, test, shell installer, SSH, Docker, service or network action was executed by this reviewer.

The patcher requires exactly one occurrence of the reviewed legacy-cleanup block and rejects unknown, duplicate or already-patched input. Its replacement checks the PREROUTING listing separately, uses an empty-success `awk` filter, and retains the existing scoped deletion behavior. Static comparison against the exact 1306 template confirms a single reversible block change, with token/certificate/private-key/server-configuration placeholders unchanged. The preparation helper pins the patcher hash and preserves `install.original.sh`, patched `install.sh` and the actual unified-diff filename **`install.patch`**. Original, patched, patcher and receipt hashes are retained; the install decision binds the original/patched bytes, receipt and install-helper hash.

Independently authenticated the existing parent-executed fragment evidence: **5 tests, exit 0, all 5 named cases passed**. Actual log, receipt, test source and patcher hashes agree. The cases reproduce the original clean-host exit 1; prove corrected empty cleanup reaches the continuation marker; delete only the unmarked legacy tunnel destination while preserving FTTH-marked, unrelated and other-subnet rules; propagate listing failure 13 before continuation/deletion; and reject unknown/duplicate/already-corrected patch input. The test uses a temporary stub `iptables` and a file marker for later service steps. It does not install packages, start a service, or establish a tunnel.

Both helpers now accept an absent server directory, reject a nonempty one and legacy configuration, reject a TCP/UDP 1194 listener, check owned sync/firewall units and their loaded state, check the sysctl/helper/OpenVPN drop-in paths, and inspect routes/rules. Installation repeats these checks immediately before executing the approved installer. Final OpenVPN, timer, firewall and listener readiness is checked again after the firewall idempotence restart. These source changes close R1's late/stale conflict checks; actual host preflight results remain pending.

The generic application installer at `1306b65c34167b2d48f4817ce72990fed3b85aa3` remains unchanged and retains the pre-existing empty-cleanup defect. This private correction is only for the authorized host deployment. The actual rendered original/patch, actual certificate bytes, registration, installation, callbacks, firewall idempotence and host readiness still require their own private evidence. Enabled dependencies alone do not prove a reboot, and no physical MikroTik/device certification is claimed. Current full CI and final release acceptance remain pending.

The companion JSON records all reviewed source and evidence hashes. Relevant private files remain under `.omo/runtime/azure-ftth-setup/`; no credentials, keys, rendered installer secrets or raw HTTP data are included here.
