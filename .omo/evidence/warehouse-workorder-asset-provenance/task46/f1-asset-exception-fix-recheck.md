# F1 asset exception fixes — targeted source recheck

Reviewed validation HEAD `99ad992db31a6922ec1adf1957af81bf747426e6` plus current uncommitted changes. `f1-asset-exception-fix-source.json` pins the inspected source content. Scope: the two required corrections from `f1-asset-exception-context-review.md`, their added tests and the new browser helper wiring. No QA or product/test edits performed. No final F1 approval.

**Result: R1 and R2 are addressed in inspected source; no new blocking defect found in these deltas. Executed backend and browser evidence remains pending.**

| Finding | Recheck |
| --- | --- |
| R1: current customer scope on title POST/replay | `server/src/main/kotlin/com/duluin/ftth/inventory/application/service/InventoryAssetTitleService.kt:35–48` preserves WO/topology/assignment order, locks the historical asset, then calls the scoped customer owner lock before replay. Fresh creation still validates current assignment/title/position and now authorizes the current position location. The original customer and asset come from the sealed handover; the client cannot replace those bindings. |
| R1: current customer scope on loss POST/replay | `WarehouseAssetLossService.kt:40–58` retains saved source/destination/cutover checks, adds original saved assignment/asset/customer locking before returning replay, and uses the same scoped customer lock for fresh creation after current position validation. The customer row remains locked through the authorization decision and result. |
| Historical replay compatibility | `AssetTitleStore.lockAsset:29` only locks the tenant-scoped physical asset ID; it does not require an active installed state. The customer owner lookup uses the retained assignment installation relationship. Both successful replay branches still return their original immutable reference/view. No new current-position or title-revision requirement is placed on historical replay, and no old response/snapshot is regenerated or rewritten. |
| R1 deterministic regression coverage | `server/src/test/kotlin/com/duluin/ftth/customer/CustomerAssetExceptionContextIT.kt:136` expands `TITLE_FRESH`, `TITLE_REPLAY`, `LOSS_FRESH`, `LOSS_REPLAY`. Each waits for the command to block on an actually held customer row, commits its move to an inaccessible area, then expects 404. Replay cases first check identical allowed replay bodies. Final assertions distinguish zero versus one retained proposal and verify no title transfer, loss movement or changed installation. These test cases directly exercise the finding; execution is not asserted here. |
| R2: valid original-requester continuation | `web/src/components/organisms/customer/CustomerAssetPanel.tsx` requires approval.view in proposalAccess. An otherwise eligible loss requester lacking that permission sees a disabled entry and a specific access-management explanation. `CustomerAssetException.tsx:33` also requires approval.view before form review, and the saved fallback at line 52 correctly says that access must be granted to the original requester. It no longer suggests another actor can submit the sealed source. Backend approval ownership rules are unchanged. |
| R2 test | `CustomerAssetException.test.tsx:136` removes approval.view and checks disabled loss entry, the explanation, absence of title creation and no context request. Existing captured-command/offline/retry tests remain in the source. |

The new `web/e2e/warehouse/asset-exception-journey.ts` drives an office requester through named customer context, proposal creation, approval submission, a distinct reviewer's rejection, canRework=false and unchanged installation history. It is wired into the existing sold-title and loan-loss customer asset journeys. This is real-backend browser source, not proof that those journeys have executed successfully. Independent approval and numeric/physical journeys must still pass in the actual run.

Parent reports web 18/18 plus typecheck/lint/build PASS in session 63261. This reviewer has not independently revalidated those raw reports. Backend session 41735 was active and sources frozen; browser journeys were not yet executed. R1/R2 are therefore marked source-addressed, execution-pending. Preserve current full regression, C7 expiry, final F1–F4 and evidence-index closure requirements.
