Authenticated server artifact review for CI run 36238196382 at `cc47f434e7c2fc58ff0095feb8c643717f889e86`. The run and aggregate acceptance failed. This is evidence verification only; no local build, database probe or QA was executed.

Actual XML records **325 unique focused tests in 50 suites: 324 passed, one failed, zero errors/skips**. The report class set exactly matches that commit's focused workflow list. Both report directories contain byte-identical copies of the same 50 XML files; the copies are not another 325 tests or a full-server run. The private compatibility log independently reports the same count.

The sole failure is `WarehouseSchemaITSelectiveTiming`, test `catalog covers every deferrable warehouse validator with an internal entry assertion()`: `NoSuchElementException` at line 109 because `WarehouseDeferredGuardCatalog.expected` lacks `warehouse_count_draft_final_guard`. This is confirmed from the authenticated failure and the tested source, rather than inferred from the job label. Commit `99ad992db31a6922ec1adf1957af81bf747426e6` adds that catalog entry; its runtime outcome is a separate gate.

Server test compilation passed. The focused step failed; full server, fresh-migration and historical-upgrade gates were skipped, and executed-result verification failed. All thirteen nonserver jobs have successful metadata at cc47, but their raw artifacts were outside this cc47 server-only review. No aggregate CI or F4 pass is claimed.

Artifact 10904999174 was downloaded privately, matched its GitHub SHA-256/size, and decrypted with authenticated age using the authorized identity. All three ZIP members and all 105 decrypted raw members were compared. The adjacent JSON contains reviewed archive/report hashes and failure metadata; `f4-cc47f434-source-input-hashes.json` binds 3,282 Git source inputs to the exact tested commit. This failed server artifact contains no server JAR, so its source binding is the GitHub run/artifact identity plus source/workflow hashes, not an independent binary comparison. Raw logs, XML and decrypted contents remain private.
