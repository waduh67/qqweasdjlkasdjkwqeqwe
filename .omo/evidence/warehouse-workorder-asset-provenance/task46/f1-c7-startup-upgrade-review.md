# Bounded C7 startup and pinned upgrade source review

Source reviewed: validation checkout HEAD `a9cd278f67a9450770869587be764145cd1d7b53` plus the startup/upgrade changes captured in `.omo/runtime/draft-expiry-upgrade-r2-inputs.json`, followed by the explicitly identified selector-only change in `scripts/warehouse/qa.sh`. This is a read-only source review, not final F1 acceptance. No Gradle, Java, database, Docker or browser QA was executed by this reviewer, and no product/test source was changed.

No remaining actionable product or handoff blocker was found in this bounded review. The initial QA selector issue was reported and fixed during review. Root reports upgrade R2 passed 7 tests in 4 suites with cleanup exit 0; this note does not independently validate those raw test reports or extend that result to subsequent source changes.

| Concern | Concrete source and conclusion |
| --- | --- |
| Genuine older application | `scripts/warehouse/draft-upgrade-bootstrap.sh:4` pins `1d14f4b9aebcd1c1a9e8c84041259ff56a422005`, refuses another HEAD or tracked/unexpected untracked edits, and copies only the current test seed into that checkout. `draft-upgrade-classpath.gradle:3` obtains the pinned server test runtime classpath after `testClasses`; the `findProject`/return branch avoids incorrectly applying the task to included build-logic. Current `server/build.gradle.kts` does not add `historicalTest` to the ordinary current test source set. |
| Independent source binding | The saved bootstrap descriptor's 2,595 source hashes were independently compared with Git archive bytes at the pin and all matched. Its three current harness hashes also matched, and its classpath file hash matched. The saved classpath has 10 first-party entries under the pinned checkout, including server outputs and the pinned dependent modules, and no current-checkout runtime entries. This validates source/classpath provenance; it is not a separate execution of the bootstrap. |
| Old process closes before migration | `WarehouseDraftUpgradeProcess.kt:15` launches a distinct JVM with the saved pinned classpath, requires normal exit and the private seed file, and kills/reaps a timed-out child. `WarehouseDraftUpgradeSeed.kt:30` encloses the old Spring context in `use`; process exit follows context closure. Both upgrade tests call `seed`, then `database.migrate`, then `postingContext(database)`, in that order. There is no current application context held across the older-schema stage. |
| Isolated database and non-owner commands | `WarehouseSchemaDatabase.kt` creates a random owned fixture database restricted to the local QA endpoint, applies target migrations there, verifies shared warehouse functions were not changed, and drops the fixture on exit. The seed independently constrains the URL/allowed target and verifies `warehouse_app`, non-superuser and NOBYPASSRLS before issuing commands. Commands pass through the old Spring application and secured MockMvc controllers; this is not a browser or socket-level HTTP proof. |
| Current startup refuses missing protection | `WarehouseDraftSchemaGuard.kt:8` is an eager component dependent on database initialization; `WarehouseDraftLifetimeStore.kt:8` explicitly depends on it. It rejects missing required clock tables or absent FORCE RLS, absent named functions, and missing/disabled named source/child/approval/effect/seal/immutability triggers. The query has no compatibility fallback. The two startup tests specifically require the guard error for target 178.11 and for a disabled document liveness trigger. The guard is a capability/installation check, not an attestation of arbitrary replacement function bodies. |
| Transfer upgrade behavior | `WarehouseTransferDraftUpgradeIT.kt:16` starts the pinned process at 178.9, then requires migrations including 178.10 and 178.12. It compares durable document, line, operation, identity and movement facts before current mutations; replays old create and dispatch; requires a legacy draft baseline with a future deadline; permits revision-checked PUT for the unposted draft; dispatches the revised quantity; rejects editing the dispatched document; and confirms the dispatched durable facts and repeat-migrate no-op. |
| Count upgrade behavior | `WarehouseCountDraftUpgradeIT.kt:16` starts the pinned process at 178.10, then requires 178.11 and 178.12. It compares count scopes, entries, rounds, results, operations, identities and movements, replays the old create/start/submit response strings, requires the legacy baseline, permits only the never-started draft PUT, rejects COUNTING/POSTED edits, starts the revised draft, and checks unchanged stock/no count movements plus repeat-migrate no-op. |
| Preserved responses | Count create/start/submit and transfer create are compared as original response strings. Transfer dispatch is compared as parsed JSON; the durable operation comparison preserves the stored original body, and `WarehouseTransferService.execute` returns `prior.receipt`, while `WarehouseTransferController.response` returns `receipt.originalBody` directly. Source supports byte preservation, but this particular dispatch upgrade assertion alone proves JSON equality rather than wire-byte equality. |
| Safe retained evidence | Child seed files and diagnostics stay under private runtime folders; seed file permissions are 0600, enclosing folder permissions 0700, and the QA wrapper sets umask 077. The workflow adds bootstrap/handoff directories only to the encrypted raw archive; uploaded public JSON contains safe references/counts/hashes. No seed contents, credentials, raw XML or logs were copied into this review. |

The initial selector code prepared the pinned bootstrap for full suites, full class names and the plan's `*Name*` selectors, but missed valid simple class and class/method selectors. The updated block also compares the simple name and the selector with its final method component removed, preserving Bash wildcard matching. This source correction was reviewed. Root reports seven extracted selector probes and shell syntax validation passed; the original R2 manifest remains unchanged, so its execution must not be represented as testing the later selector bytes.

Evidence binding: the frozen R2 manifest contains 2,618 entries and has SHA-256 `37dc672ba1171b89acd5633d059ab7075a5ee4f152f86f9df4c8f802bf5815bf`. At initial review all 10 directly scoped startup/harness/test files matched it. At completion only `scripts/warehouse/qa.sh` differs among those 10 because of the disclosed selector correction. The private bootstrap descriptor has SHA-256 `08ab0d500dcf54cf836f9c76f5b17e7b2a0eac9582f786116b5cc426f8f69732`. Current reviewed source hashes and each available R2 comparison are in sibling `f1-c7-startup-upgrade-source.json`.

Remaining acceptance evidence belongs to the parent gate review: retain the actual nonzero R2 reports, child normal-exit handoff records and cleanup result; bind subsequent selector validation separately; rerun affected/full gates for the checkpoint source. This review does not close specialized short-clock evidence, all-family C7 behavior, tasks 46–48, or final F1.
