# Deployment foundation source review — preliminary

The checked-in deployment is not ready to start the current image safely. Four source findings are confirmed in the paired JSON; no application or migration changes are needed to correct these deployment defaults.

- `DEPLOY-F4-01` (high): `deploy/DEPLOY.md:1110` explicitly advises removing a test dependency. Replace it with repair/rerun instructions and retain all required gates.
- `DEPLOY-F4-02` (high): production validation always checks `BootstrapProperties.demoAdminPassword`, including when demo seeding is disabled. `application.yml` has no `FTTH_DEMO_ADMIN_PASSWORD` placeholder. For the immutable current image, Compose must supply the canonical Spring property environment key `FTTH_BOOTSTRAP_DEMOADMINPASSWORD`, optionally mapped from an operator-facing `FTTH_DEMO_ADMIN_PASSWORD` setting. Keep production validation enabled and demo seeding disabled. A short environment variable alone will leave the rejected Kotlin default in effect.
- `DEPLOY-F4-03` (high): the checked-in init script makes `FTTH_DB_USER` the database owner, and Flyway uses the same credentials because Compose supplies no separate Flyway settings. This violates required runtime owner separation and fails `scripts/warehouse/preflight.sql:40`. Current migrations explicitly restrict protected clock tables and grant expiry functions to `warehouse_app`. Use that non-owner runtime role, a separate migration/database owner and explicit `SPRING_FLYWAY_URL/USER/PASSWORD`; preserve current restricted grants and deny runtime membership in owner roles. Do not repair this with broad post-migration grants or edited applied migration bytes.
- `DEPLOY-F4-04` (medium): `.env.example` supplies the rejected short storage access key and a minimum-eight secret comment. Current production validation requires both storage keys to be non-placeholder values of at least16 characters. Update the template and generate independent private secrets.

The current workflow dependency graph is intact: deploy.build-and-push requires the reusable warehouse-verification workflow; deploy requires build-and-push; warehouse.acceptance requires exactly server, web, shared, browser, legacy-upgrade, native and images, and rejects missing/failed/skipped/cancelled results. Publication validates the exact commit, image archives and image IDs before registry writes. The operational archive-load route authorized by the user must retain equivalent evidence gates and must not modify this publication contract or relabel1306 images as a later configuration commit.

Evidence reuse is appropriate only with an explicit composed release identity: immutable1306 application/image source plus a separately hashed deployment configuration revision, overlay and non-secret effective settings. The existing all3693-file manifest is not wholly equivalent after deployment edits; enumerate every delta and independently prove all application/build/test/workflow/migration inputs unchanged. Keep current unfiltered1306 CI running and authenticate its complete focused/full/historical/protocol results before release. Existing current24 browser,6legacy,607web,44shared,native2 and8preflight reports remain source-bound to1306. The existing image smoke omits FTTH_PRODUCTION=true and therefore does not establish production startup. Do not claim it does.

Required affected verification, to be executed by root under the authorized setup scope:

1. Review the exact final Compose/template/init/proxy/docs changes and rerun their syntax/guard checks. Privately render the complete Compose plus host overlay and verify required environment names are present; retain only safe hashes and booleans. Verify runtime image IDs, archive checksums, architecture and revision match the authenticated1306 saved archives. Loading an existing archive is distinct from registry publication and rebuilding.
2. Start the exact images against a fresh, task-owned database using the final role initialization and actual production flags. Verify real backend JSON readiness, platform bootstrap with no demo tenant, object-storage connectivity, restricted application role, current migration367/178.12, trusted draft-clock table/function permissions and restart with unchanged image IDs. Exercise missing/weak required credentials as expected startup/config rejection on an isolated instance. Production readiness requires actual startup; parsing environment variables is insufficient.
3. Preserve the authenticated isolated legacy eight-probe preflight as code/migration evidence. Test new initialization/role wiring through the documented isolated preflight and representative authenticated read/write/replay after restart; do not fabricate stock with SQL. Keep raw login/environment/database evidence private. A fresh production database must not inherit fixture tenants or fixture credentials.
4. Use a separate FTTH Compose project and volumes. Existing unrelated Caddy already owns80/443, so do not start a competing public listener or overwrite its configuration. Review an explicit host overlay and unique proxy target alias; attach only the required FTTH gateway/services to the existing edge network. Preserve existing routes and verify both the old application and FTTH HTTPS after the controlled edge change. Private database, storage, backend and GenieACS NBI ports remain unpublished. DNS/TLS and Azure rules are runtime checks outside current CI.
5. Hash final deployment files/overlay, public image identities and safe startup/preflight receipts into a release manifest. Keep rollback forward-only for applied schema and preserve backups; do not reset existing host data or downgrade Flyway history. Re-review task47/task48 and F1/F2/F4 affected scope before final acceptance. No main merge or image publication is authorized by this setup request.

For the requested domain, public TCP80/443 serve HTTP redirect/certificate validation and HTTPS; restrict TCP22 to operator source addresses. Add UDP1812/1813 only when RADIUS is enabled and restricted to NAS sources, TCP7547/7567 only for configured CPE functions and intended access networks, and TCP8880 only if the walled-garden endpoint is intentionally enabled. Do not broadly open internal PostgreSQL5432, backend8080, MinIO9000/9001 or GenieACS7557. This is source-derived port guidance, not an assertion that Azure NSG or remote listeners were inspected by this reviewer.

Reviewer work was read-only source/artifact analysis. No SSH, local QA, Docker/image load, remote mutation, publication or cancellation was executed. This is not final F4 approval. The paired JSON records exact source hashes and current authenticated image IDs.
