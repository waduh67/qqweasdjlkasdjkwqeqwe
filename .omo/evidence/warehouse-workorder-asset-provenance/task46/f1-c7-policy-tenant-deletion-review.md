# Bounded C7 policy-only empty tenant deletion review

Reviewed validation HEAD `a85b3fda952e16a38913711f03ee8c8819382cc1` plus the uncommitted `TenantEraser.kt` and `DeleteTenantIT.kt` changes. No product edits or QA were performed. This is source review only, not final F1 acceptance. The unrelated running backfill/upgrade work is outside this review.

No scope, privilege or protected-history bypass was found in the reviewed product delta. It implements the explicit owner decision that otherwise-empty tenants remain deletable, while tenants with protected history must use Suspend.

| Check | Concrete source and conclusion |
| --- | --- |
| Narrow control exemption | `server/src/main/kotlin/com/duluin/ftth/common/infrastructure/persistence/TenantEraser.kt:59` uses the same filtered table list for protected-history inspection and manual deletion. The exclusion at line 177 is exactly `inventory_tenant_cutover`, `iam_authorization_epoch` and `inventory_draft_policy`. It does not broadly exempt draft tables or inventory tables. |
| Policy is configuration | Existing immutable `V178_12__warehouse_idle_draft_expiry.sql:6` defines policy versions with tenant FK `ON DELETE CASCADE`. The unused policy row contains the TTL/version/configuration observation, without stock or title effects. Its presence alone should not become transaction history. |
| Protected C7 history still blocks deletion | Document/plan activity, expiry, receipt canonical commands, operations and all other protected business tables remain in the dynamic history scan. V178.12 grants only SELECT on activity/expiry tables, and receipt commands remain append-only. The scanner detects missing DELETE privilege or the append-only delete trigger before deleting business data. Activity-to-policy foreign keys have no delete cascade; ignoring the policy itself does not remove or detach referenced history. |
| Direct application deletion remains denied | No grant or SQL migration changes are made. V178.12 revokes app privileges then grants only SELECT on policy; its update trigger is append-only. Its delete trigger invokes `warehouse_tenant_control_delete_guard`, defined by immutable V178.9, which checks tenant scope and rejects deletion while the tenant still exists. The final tenant-row FK cascade is the intended cleanup path. |
| Tenant scope and race fence retained | The eraser still opens a target-tenant REQUIRES_NEW transaction and obtains tenant `FOR UPDATE` at line 52 before scanning. New child insertion through tenant FKs cannot race past the history decision. Existing C7 activity and source FKs remain present. The final tenant deletion remains in that same transaction. |
| API authorization unchanged | `TenantController.kt:60` retains `platform.tenant.delete`; `TenantService.kt:99` still rejects deletion of the platform tenant. The patch adds neither an owner connection nor a privilege bypass to the product eraser. The owner connection appears only in the new test fixture setup. |

The new test is meaningful for the regression: create an otherwise-unused policy, require direct app policy DELETE to fail, delete through the platform endpoint, require 204/404 and zero rows across all tenant-scoped tables. The existing ordinary cleanup/cross-tenant preservation, protected ONU history rejection, and platform-tenant rejection cases remain. The reviewed test's owner INSERT lacks the explicit tenant GUC required by the existing policy capture trigger; root identified this before requesting review and is fixing the fixture after the active freeze. This is not a product defect, and no passing result is claimed for these test bytes.

Acceptance still needs the corrected fixture's actual nonzero deletion suite result, with direct policy deletion denied and protected-history rejection retained. The new policy-only case does not by itself constitute executed coverage for deleting a tenant containing C7 activity; source review confirms those tables remain protected. Current hashes are recorded in sibling `f1-c7-policy-tenant-deletion-source.json`. V178.12 remains unchanged; no new migration is needed for this application-only correction.
